import { RepeatStringPipe } from './repeat-string.pipe';

describe('RepeatStringPipe', () => {
  it('create an instance', () => {
    const pipe = new RepeatStringPipe();
    expect(pipe).toBeTruthy();
  });
});
