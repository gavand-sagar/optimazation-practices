import { MyDebounceDirective } from './my-debounce.directive';

describe('MyDebounceDirective', () => {
  it('should create an instance', () => {
    const directive = new MyDebounceDirective();
    expect(directive).toBeTruthy();
  });
});
